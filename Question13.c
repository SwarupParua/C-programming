/*write a c program to calcualte area of a rectangle :
>>using hard coded inputs.
>>using input supplied by the user. */

#include<stdio.h>

int main(){
    int length=4,breadth=8;
    int area=length*breadth;
    printf("Area of the rectangle is :%d",area);
    return 0;
}