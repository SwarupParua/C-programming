/* Write a C program to find an ASCII value of a character. */

#include<stdio.h>
int main()
{
    char c;
    printf("Enter a single character value is :");
    scanf("%c",&c);
    printf("ASCII code of the character value is :%d",c);

    return 0;
}